define(
    ["app"],
    function (app) {
        app.factory("UtilService", [
            function () {
                var utilityService = {};

                return utilityService;
            }
        ]);
    });